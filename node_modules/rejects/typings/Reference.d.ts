export declare enum ReferenceType {
    ARRAY = "arr",
    OBJECT = "obj"
}
export default class Reference extends String {
    static is(str: string | Reference): boolean;
    type: ReferenceType;
    key: string;
    constructor(key: string, type?: ReferenceType);
}
